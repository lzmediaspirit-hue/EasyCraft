# EasyCraft — הוראות לסוכן קוד

אפליקציית ניהול לנגרייה, בעברית, שעובדת בלי אינטרנט. React 19 +
TypeScript + Vite + Tailwind v4 + Dexie (IndexedDB).

`README.md` הוא התיעוד המלא — מה קיים, איך מחושבות הפלטות, ואיך
עובד מנוע ההדמיה. הקובץ הזה הוא מה שצריך לדעת **לפני** שנוגעים בקוד.

## פקודות

```sh
npm install         # PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 אם אין צורך בבדיקות
npm run dev         # שרת פיתוח על 5173
npm run typecheck   # חייב לעבור לפני כל קומיט
npm run build:single  # קובץ HTML יחיד, ASCII בלבד
npm test            # 120 חבילות Playwright — דורש שרת פיתוח פעיל
npm run test:contract  # חבילות החוזה בלבד — מה שעוצר פרסום
```

`npm test` דורש דפדפן Chromium ושרת על 5173. בסביבה בלי דפדפן הוא
ייכשל מיד, וזה לא באג — ראה `tests/README.md`.

## חוקים שאסור לשבור

**הכול במ״מ.** כל מידה בבסיס הנתונים ובחישובים היא מילימטרים.
סנטימטרים הם שפת תצוגה בלבד, ב-`src/ui/units.ts`. מספר שנכנס בלי
להיות מומר הוא באג שקט שמתגלה רק בניסור.

**RTL מהיסוד.** `start`/`end` ולא `left`/`right`, ב-CSS וב-Tailwind
כאחד (`ms-2` ולא `ml-2`). מספרים מקבלים `className="num"`, שמבודד
אותם ל-LTR — בלעדיו "300 x 600" מתהפך.

**עברית בכל מה שהמשתמש רואה**, וגם בהערות ובהודעות הקומיט. זה
הפרויקט של נגר, לא ספרייה בינלאומית.

**אין מריץ בדיקות.** כל חבילה היא סקריפט node שמדפיס PASS/FAIL.
לא להוסיף vitest, jest או playwright test runner — הבדיקות מריצות
את האפליקציה האמיתית בדפדפן אמיתי, וזו הנקודה.

**בדיקה אמיתית ולא מדומה.** לא למוק את בסיס הנתונים ולא את הדפדפן.
באג של "הארגז נשבר בזווית 135 מעלות" לא קיים בשום פונקציה בודדת.

**חבילה שנפלה היא כישלון.** `tests/_all.sh` בודק את קוד היציאה ולא
רק את הטקסט, כי חבילה שקרסה לפני ההשוואה הראשונה שלה לא מדפיסה FAIL.
לא להחזיר ספירת טקסט בלבד.

**מה שנמחק לא חוזר.** `seedCatalog` זורע רק לספרייה ריקה. זריעה של
"מה שחסר" בכל טעינה החזירה ארגזים שהמשתמש מחק, והחזרה לזה שוברת כל
נגרייה שהחליפה את הספרייה בשלה. הדרך חזרה היא `catalogRepo.reseed()`,
והיא מפורשת.

## מקורות אמת יחידים

מה שמחושב בשני מקומות מתחיל להיות שני דברים שונים. הקבצים האלה
קיימים כדי שזה לא יקרה, ואין לעקוף אותם:

| קובץ | השאלה שהוא עונה עליה |
|---|---|
| `features/design/placement.ts` | איפה הארגז עומד בחדר |
| `features/design/collision.ts` | האם מותר להניח אותו שם |
| `features/design/snapping.ts` | לאן הוא קופץ בכל ציר בנפרד |
| `features/design/stacking.ts` | לאיזו פינה הוא נוחת כשמניחים אותו על ארגז |
| `materials/boardSpec.ts` | מה זה הלוח שנבחר, ומה מצבו המסחרי |
| `features/design/isoMath.ts` | מה נראה, ומה מצויר לפני מה |
| `catalog/standards.ts` | מידות התקן בנגרות, עם נימוק לכל מספר |
| `db/types.ts` | המודל כולו, וגם הפונקציות שנגזרות ממנו |

## מבנה

```
src/db/          מודל הנתונים ו-IndexedDB. מיגרציות עולות, לא מוחקות
src/catalog/     ספריית המוצרים, מידות תקן, ומנוע איורי הארגזים
src/materials/   ליבות, גוונים, והמחיר שבהצטלבות שביניהם
src/costing/     פירוק לחלקים, ניסור, וחישוב מחיר
src/workflow/    שלבים, תפקידים, והרשאות
src/features/    מסך לכל תחום, עם שכבת גישה משלו
src/ui/          רכיבים משותפים ויחידות מידה
```

**שכבת גישה.** `db` מיובא בשכבת הנתונים בלבד — `src/db/` וקבצי
`*Repo.ts`. מסך לא ניגש ל-Dexie ישירות, ולא דרך עוקף: זה נבדק
בקוד עצמו (l125), וייבוא חדש מחוץ לשכבה מפיל את הבדיקה. קריאות
חיות דרך `useLiveQuery`.

**בעלות, גרסה וסימוני מחיקה.** לכל שורה יש `workshopId` ו-`rev`,
והמחיקה משאירה שורה ב-`tombstones`. שלושתם עוברים דרך `db/rows.ts`
בלבד — `owned()` לשורה חדשה, `patchRow` לעדכון, `eraseRows`/`eraseIds`
למחיקה, ו-`allMine`/`onlyMine`/`mine` לשאילתה. אלה אינם קישוט
לעתיד: בלי בעלות אין מה להגביל בשרת, בלי גרסה אין איך להכריע בין
שני שינויים, ובלי סימון מחיקה שורה שנמחקה חוזרת מהמכשיר השני.
הנגרייה הפעילה נקבעת ב-`db/workshop.ts`. לא להוסיף כתיבה שעוקפת
את העוזרים האלה.

**מיגרציות.** `db.version(N)` בלבד; לא לשנות גרסה קיימת. למשתמש יש
נתונים אמיתיים במכשיר, ושינוי רטרואקטיבי הורס אותם.

## מה לא בריפו, במכוון

- `dist/` — נוצר מהמקור.
- `tests/shots/` — צילומי מסך של הבדיקות.
- קבצים מצורפים של פרויקטים — הם Blob ב-IndexedDB.

## אבטחה

יש חשבון ברירת מחדל `admin` / `admin2026`, שנקבע בבקשת בעל הפרויקט.
זו נקודת פתיחה ולא הגנה: הסיסמה ידועה לכל מי שראה את הקוד, והנתונים
יושבים ב-IndexedDB ללא הצפנה. מסך הכניסה ומסך הצוות מסמנים אותה עד
שהיא מוחלפת. לא להסיר את הסימון הזה, ולא להציג את האפליקציה כמאובטחת.
