node_modules
dist
.DS_Store
*.local

# צילומי מסך של הבדיקות — נוצרים מחדש בכל הרצה.
#
# הדפוס היה `*.png` לכל הריפו, וזו מלכודת: ביום שבו תיכנס תמונה
# אמיתית לאפליקציה היא פשוט לא תיכנס לגיט, בלי שאיש יראה למה.
# מה שנוצר מחדש יושב בתיקיות האלה, ורק הן מסוננות.
tests/shots/
shots/
